<?xml version="1.0" encoding="UTF-8" ?>
<!--TO ADD POSITION IN POS CONFIG-->
<odoo>
    <record id="pos_config_view_form" model="ir.ui.view">
        <field name="name">pos.config.view.form.inherit.pos.screen.pane.position
        </field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='other_devices']" position="after">
                <setting id="screen_numeric_buttons_visible" string="Hide numeric buttons"
                         help="If the option is checked it will be hidden">
                    <field name="numeric_buttons_visible"/>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
